/* ==========================================================
   markdown-loader.js
   Optional Markdown Loader
   ==========================================================

   This file is reserved for loading Markdown project
   files dynamically in the future.

   Current version:
   GitHub Pages links directly to each Markdown file,
   so no additional JavaScript is required.
*/

console.log("Markdown loader initialized.");
